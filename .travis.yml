language: java

# explicit distribution specification
# to use xvfb as a service
dist: xenial

jdk: openjdk8

git:
  depth: false

services:
   - docker
   - xvfb

addons:
  sonarcloud:
    organization: "josephthachilgeorge"

# skip installation step
install: true

cache:
   directories:
      - $HOME/.m2
      - $HOME/.sonar/cache

script:
   - mvn -f sonar-example/pom.xml -Pjacoco clean verify sonar:sonar
